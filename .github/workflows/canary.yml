name: Canary artifacts

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

permissions: {}

jobs:
  canary-ci:
    uses: ./.github/workflows/desktop-build.yml
    with:
      run_tests: false
    permissions:
      contents: read
